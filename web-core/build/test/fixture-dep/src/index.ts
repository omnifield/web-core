// Зависимость взята ради формы поставки (сырой JSX по условию `solid`, CommonJS в глубине), а
// не ради разбора markdown — см. FAQ.md.
import { SolidMarkdown } from "solid-markdown";

export const parser: unknown = SolidMarkdown;
