# Адрес реестра пакетов @web-core (полигон выпуска, см. PUBLISHING.md).
# Токен сюда НЕ кладут: он в ~/.npmrc и в .env — секрету в репозитории не место.
@web-core:registry=https://150.251.145.87:9873/

# Токена здесь НЕТ и быть не может: pnpm 11 НЕ разворачивает переменные в учётных данных из
# project-level `.npmrc` — файл коммитится, и секрет утёк бы в чужой реестр (проверено
# 2026-09-23, pnpm сам отказывает предупреждением). Токен кладётся в пользовательский
# `~/.npmrc` (у нас — из `REGISTRY_TOKEN` корневого `.env`), у девопсов — шагом пайплайна.
# Разбор — `web-core/release/README.md`.
