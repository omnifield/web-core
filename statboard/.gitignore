node_modules/
dist/

# Дерево маршрутов собирает плагин при запуске и сборке — в гит не кладём.
src/routeTree.gen.ts

# Кэш графа задач — локальный, у каждого свой.
.nx/

# Локальные env: адреса и токены. Не в гит.
.env
.env*.local
