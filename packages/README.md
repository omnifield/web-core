# 🧱 Пакеты фреймворка

🗂️ каталог монорепо · 📦 npm-скоуп `@web-core/*` · 🚚 поставка одной версией

## 🧭 Навигация

- 🏠 [Главное](#главное)
- 🚪 [Критерий включения](#критерий)
- 📋 [Состав](#состав)
- 📏 [Общие правила группы](#правила)
- ➕ [Как завести новый пакет](#новый)
- ❓ [FAQ](./FAQ.md)
- 🗺️ [ROADMAP](./ROADMAP.yaml)

<h2 id="главное">🏠 Главное</h2>

🧱 База фреймворка: движки, оснастка и примитивы, из которых собирается всё остальное в
репозитории — приложения (`../apps/`), межпродуктовые интерфейсы (`../interfaces/`), продукты
(`../products/`). 📦 Каждая папка здесь — самостоятельный npm-пакет под скоупом `@web-core/*`,
который **уезжает потребителю в реестр** и живёт своей жизнью после публикации.

🚚 Отсюда главное свойство группы: пакет пишется не «для нашей витрины», а для чужого проекта,
которого мы не видели. Потребитель ставит его из реестра, держит СВОЮ версию `solid-js`, свою
базу `tsconfig` и свой бандлер — и всё, что мы забыли проверить на границе поставки, всплывает
у него, а не у нас.

<h2 id="критерий">🚪 Критерий включения</h2>

✅ **Сюда кладут** механизм, у которого есть все три признака сразу:

1. **Публикуемая поверхность** — пакет с `exports`, который имеет смысл поставить из реестра
   в проект, ничего не знающий про этот репозиторий.
2. **Свой предмет, а не сборка чужих** — пакет владеет одной механикой (состояние, роутинг,
   скин, сборка дерева) и объясняет себя без ссылки на конкретное приложение.
3. **Нулевое знание о потребителе** — не импортирует ничего из `../apps/`, `../interfaces/`,
   `../products/` и не упоминает их в доке (исключение — пример со СТАБИЛЬНОЙ обвязкой, см.
   правила документации в корневом [`README.md`](../README.md)).

🚫 **Сюда НЕ кладут:**

- **готовый UI, сводящий несколько пакетов под общий интерфейс** — это `../interfaces/`
  (`feeder`, `chat`): они стоят НАД базой и знают, что с чем соединяют;
- **поверхность, которую открывают в браузере** — это `../apps/`: у приложения адрес, а не
  импорт-путь;
- **сервис со своим портом и сетевым контрактом** — это `../backend/`;
- **продукт, который фреймворк обслуживает** — это `../products/` (временно здесь, уедет своими
  репозиториями);
- **знание предметной области без кода** — это `../canons/`.

<h2 id="состав">📋 Состав</h2>

📚 Одна строка — одна сущность; устройство, решения и план каждой живут в её собственной доке и
здесь не пересказываются.

| Пакет | Группа · genus | Назначение |
|---|---|---|
| [`assembly`](./assembly/README.md) | assembly · engine | дерево узлов, реестр из паспортов, правки дерева, отрисовка по данным |
| [`build`](./build/README.md) | build · tooling | фабрика конфига Vite, база TypeScript, пресет vitest |
| [`form`](./form/README.md) | forms · engine | валидация и жизнь формы поверх дерева сборки |
| [`generators`](./generators/README.md) | codegen · engine | что штампует шаблон в этом репозитории и держит его актуальным |
| [`io`](./io/README.md) | data · engine | универсальный вход/выход данных фреймворка, граница `zod` |
| [`lint`](./lint/README.md) | quality · engine | пресет `eslint-plugin-solid` — канон Solid, выраженный машиной |
| [`mcp`](./mcp/README.md) | mcp · tooling | тулинг MCP-серверов зон: регистрация тула, конверт ответа, транспорт, пагинация |
| [`neurobox`](./neurobox/README.md) | ai-runtime · engine | рантайм агента: тулы, сессии, транспорт, прогоны |
| [`query`](./query/README.md) | data · engine | данные из сети поверх `@tanstack/solid-query` плюс GraphQL/REST-транспорт |
| [`router`](./router/README.md) | routing · engine | роутинг поверх `@tanstack/solid-router` |
| [`skin`](./skin/README.md) | skin · engine | модель скина, сборка рецепта, адресация из анатомии, порождение CSS, надевание |
| [`solid`](./solid/README.md) | framework · engine | единственная точка резолва `solid-js`/`solid-js/web` плюс `mountApp()` |
| [`store`](./store/README.md) | state · engine | состояние поверх `@xstate/store` |
| [`style`](./style/README.md) | style · engine | токен-контракт, шкалы, построение половин из семени, Solid-обвес |
| [`trace`](./trace/README.md) | diagnostics · engine | движок perf-трейсов: ядро без фреймворка плюс необязательный Solid-плагин |
| [`ui`](./ui/README.md) | kit · component | примитивы на Ark/Zag — 1-to-1 DOM, ноль стилей по умолчанию, паспорт компонента |

<h2 id="правила">📏 Общие правила группы</h2>

Всё перечисленное — обязанность КАЖДОГО пакета, а не пожелание.

- 🧾 **Своя тройка** `README.md`/`FAQ.md`/`ROADMAP.yaml` по шаблону своего `genus` (шаблоны и
  пилоты — корневой [`README.md`](../README.md), раздел «Документация»).
- 🏷️ **Шапка `группа · genus · пакет`** сразу под `# H1` — по ней читатель понимает вид сущности
  до того, как начнёт читать.
- 🔗 **Соседей берём `workspace:*`**, не ручным диапазоном: ручной диапазон на свой же пакет
  некому пересматривать при подъёме версии соседа, и он расходится молча (разбор —
  [`FAQ.md`](./FAQ.md)).
- 🚚 **Единая версия поставки.** Пакеты этой группы перечислены в `versioning.fixed`
  (`../pnpm-workspace.yaml`) и поднимаются одним номером: бампается один — бампаются все.
- 📮 **Реестр один** — `@web-core:registry` из корневого `.npmrc`; публикация и её приёмка
  описаны в [`PUBLISHING.md`](../PUBLISHING.md).
- 🌐 **Граница поставки проверяется СНАРУЖИ.** Внутри монорепо пакеты соединены симлинками, и
  ошибки поверхности (`exports`, `files`, типы из `dist`, диапазоны зависимостей) здесь не видны
  вовсе — их ловит только установка опубликованного пакета в чистый проект.
- 🧪 **Проверки зелёные перед коммитом**: `pnpm --filter @web-core/<пакет> lint typecheck test`.

<h2 id="новый">➕ Как завести новый пакет</h2>

1. Проверить по [критерию включения](#критерий), что это правда база фреймворка, а не интерфейс,
   приложение или продукт.
2. Завести папку, `package.json` со скоупом `@web-core/*`, оснастку из [`build`](./build/README.md)
   (три конфига по три строки, без самописной обвязки).
3. Написать тройку доков по шаблону своего `genus` — по факту кода, не вперёд него.
4. Добавить пакет в `versioning.fixed` (`../pnpm-workspace.yaml`) — иначе он выпадет из общей
   поставки и разойдётся по версии с остальными.
5. Завести зону владельца в `../.claude/harness.yaml`: одна папка — один владелец.
6. Добавить строку в [состав](#состав) этого файла.
