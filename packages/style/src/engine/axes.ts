import {
  CONTROL_TARGET_MIN,
  DENSITY_CEILING,
  DENSITY_FLOOR,
  DENSITY_TOKEN,
  DERIVED_SCALES,
} from "./dimension.js";

export type BoundKind =
  | "норма"
  | "предел поддержки"
  | "практический предел"
  | "границы нет";

export interface AxisBound {
  value: number | null;
  kind: BoundKind;
  norm: string | null;
  why: string;
}

export interface AxisReference {
  value: number;
  market: string;
}

export interface Axis {
  token: string;
  unit: string;
  reference: AxisReference;
  floor: AxisBound;
  ceiling: AxisBound;
  continuous: boolean;
}

const controlSmallest = DERIVED_SCALES.find((scale) => scale.seed === "control-height")!.steps.find(
  (step) => step.name === "control-height-sm",
)!;

const CONTROL_FACTOR = "factor" in controlSmallest ? controlSmallest.factor : Number.NaN;

const CONTROL_SEED_FLOOR = Number.parseFloat(CONTROL_TARGET_MIN.value) / CONTROL_FACTOR;

const unbounded = (why: string): AxisBound => ({ value: null, kind: "границы нет", norm: null, why });

export const AXES: readonly Axis[] = [
  {
    token: DENSITY_TOKEN,
    unit: "множитель",
    reference: {
      value: 1,
      market:
        "единица — это «сто процентов»: вид ровно такой, каким его сверяли с рынком. Ту же роль у Radix Themes играет scaling 100% при диапазоне 90–110% (сверено 2026-09-23)",
    },
    floor: {
      value: DENSITY_FLOOR,
      kind: "норма",
      norm: "WCAG 2.2, 2.5.8 Target Size (Minimum), AA",
      why: "нижняя ступень контрола (`семя × 0.8 × плотность`) обязана остаться не ниже минимального размера цели 24×24 CSS-пикселя. При нынешнем семени 2.5rem это даёт ровно 0.75, и запаса на этой границе нет: она посчитана, а не выбрана",
    },
    ceiling: {
      value: DENSITY_CEILING,
      kind: "предел поддержки",
      norm: null,
      why: "верх диапазона, на котором прогоняются пробы и который зона обещает. Крупнее ничего не нарушает — норма сверху не ограничивает вовсе; выше просто кончаются наши проверки, а не законность значения",
    },
    continuous: true,
  },
  {
    token: "radius",
    unit: "rem",
    reference: {
      value: 0.5,
      market: "`radius-lg` равен семени без смещения, и 0.5rem — ровно `--radius-lg` у Tailwind (сверено 2026-09-23)",
    },
    floor: unbounded(
      "прямой угол (0) законен и требований к скруглению нет ни одного. Отрицательного радиуса не бывает по правилам CSS, но это правило языка, а не наша граница",
    ),
    ceiling: {
      value: null,
      kind: "практический предел",
      norm: null,
      why: "радиус больше половины меньшей стороны элемента даёт пилюлю — дальше форма не меняется. Это свойство геометрии, а НЕ требование доступности, и числом база его назвать не может: половина зависит от элемента, а не от токена",
    },
    continuous: true,
  },
  {
    token: "space",
    unit: "rem",
    reference: {
      value: 0.25,
      market:
        "шаг сетки интервалов, на котором стоит весь рынок: `--spacing` у Tailwind — 0.25rem, база Fluent 2 — 4px, шкала Material 8dp — две таких ступени. Роли отступов (`SPACE_ROLES`) сверены с рынком именно при этом семени (сверено 2026-09-23)",
    },
    floor: unbounded(
      "шаг ритма отступов ничем не нормирован. Разнесение целей 2.5.8 нормирует, но нормирует оно ЦЕЛИ в готовом оформлении, а не шаг шкалы: расстояние между целями складывается у потребителя, и отвечает за него он",
    ),
    ceiling: unbounded("сверху ритм не ограничен ничем: просторнее не хуже"),
    continuous: true,
  },
  {
    token: "font-size",
    unit: "rem",
    reference: {
      value: 1,
      market:
        "умолчание браузера, то есть единственный кегль, который уважает настройку пользователя без пересчёта. Плотные наборы рынка (Primer, Fluent 2, Atlassian) стоят на 0.875rem — это низ живой полосы, а не опора (сверено 2026-09-23)",
    },
    floor: unbounded(
      "минимального кегля у WCAG 2.2 нет ВОВСЕ (сверено 2026-08-18). Единственный критерий про размер текста — 1.4.4 Resize Text — требует возможности увеличить текст вдвое, а не запрещает мелкий; вся шкала выражена в rem, поэтому настройка пользователя уважается при любом семени. Мелкий кегль остаётся решением того, кто его поставил",
    ),
    ceiling: unbounded("крупнее не хуже: сверху ни один критерий не ограничивает"),
    continuous: true,
  },
  {
    token: "column",
    unit: "rem",
    reference: {
      value: 0.5,
      market:
        "средняя ширина знака у пропорциональной гарнитуры — примерно половина кегля: `1ch` меряет ширину нуля и у пропорциональных начертаний шире средней буквы на 20–30% (Meyer, 2018). При этом семени идеал измерения Bringhurst'а (66 знаков) даёт 33rem, и весь ряд ступеней остаётся внутри полосы 45–75 знаков (сверено 2026-09-23)",
    },
    floor: unbounded(
      "семя — средняя ширина знака у гарнитуры темы, а не выбор вида: связывающей нормы у него нет и быть не может. Поставить его неверно можно — тогда число в имени ступени перестанет соответствовать действительности, но это расхождение с фактом шрифта, а не с нормой",
    ),
    ceiling: unbounded(
      "сверху семя не ограничено. Ограничена сама КОЛОНКА, и не нашим потолком: 1.4.8 Visual Presentation (AAA) держит строку не длиннее 80 знаков — это про ступени, а не про семя. Верхняя ступень ряда (56 знаков) остаётся на 24 знака ниже потолка и внутри канона измерения (45–75 знаков, идеал 66 — Bringhurst); ступени под идеал в ряду нет",
    ),
    continuous: true,
  },
  {
    token: "control-height",
    unit: "rem",
    reference: {
      value: 2.5,
      market:
        "не рыночное число, а ВЫВЕДЕННОЕ: только при этом семени нижняя ступень держит минимум цели 24×24 на всём диапазоне плотности (`2.5 × 0.8 × 0.75 = 1.5rem`), и из него же посчитан пол оси плотности. При плотности 1 это 40px — крупный контрол у Primer и кнопка Material; плотные 32px рынка берутся плотностью около 0.8, а НЕ уменьшением семени: семя ниже 2.5rem отнимает право крутить плотность вниз (сверено 2026-09-23)",
    },
    floor: {
      value: CONTROL_SEED_FLOOR,
      kind: "норма",
      norm: "WCAG 2.2, 2.5.8 Target Size (Minimum), AA",
      why: "нижняя ступень равна `семя × 0.8` и обязана быть не ниже 1.5rem уже при плотности 1 → семя ≥ 1.875rem. Пол ПОДВИЖЕН: он растёт вместе с уменьшением плотности, и на нижней границе оси (0.75) равен 2.5rem — ровно нынешнему семени. Крутить семя и плотность вниз ОДНОВРЕМЕННО нельзя: границы не складываются, они перемножаются",
    },
    ceiling: unbounded("выше нормы нет: цель крупнее минимума нормой не ограничена"),
    continuous: true,
  },
  {
    token: "rail",
    unit: "rem",
    reference: {
      value: 16,
      market:
        "`rail-md` равен семени: 256px — привычная ширина боковой панели, и вместе с `rail-sm` (192px) она остаётся внутри потолка Material для ящика навигации (280dp на телефоне, 320dp на планшете). Ступени от `rail-lg` этот потолок осознанно превышают — он про выдвижной ящик на узком экране, а не про настольную панель (сверено 2026-09-23)",
    },
    floor: unbounded(
      "рельса нормой снизу не связана: 2.5.8 нормирует размер ЦЕЛИ внутри неё (кнопка, иконка), а не ширину самой панели",
    ),
    ceiling: unbounded(
      "сверху рельса не ограничена: шире панель — меньше место контенту рядом, но это компромисс раскладки, а не нарушение нормы",
    ),
    continuous: true,
  },
  {
    token: "card",
    unit: "rem",
    reference: {
      value: 24,
      market:
        "`card-md` равен семени: 24rem — это `--container-sm` у Tailwind, и 384px попадает в полосу ширины диалога рынка (Material держит её между 280 и 560dp) (сверено 2026-09-23)",
    },
    floor: unbounded("минимальной ширины карточки норма не задаёт: содержимое внутри переносится, а не обрезается"),
    ceiling: unbounded("сверху не ограничена: широкая карточка ничего не нарушает"),
    continuous: true,
  },
  {
    token: "layout",
    unit: "rem",
    reference: {
      value: 80,
      market:
        "`layout-md` равен семени: 80rem — `--container-7xl` и одновременно контрольная точка `xl` у Tailwind, то есть привычная верхняя граница области раскладки на настольном экране (сверено 2026-09-23)",
    },
    floor: unbounded(
      "нижняя граница области раскладки нормой не связана — это вопрос композиции страницы, а не доступности",
    ),
    ceiling: unbounded(
      "верхняя граница тоже не нормирована: 1.4.8 (AAA) ограничивает длину СТРОКИ текста (см. `column`), а не ширину области раскладки в целом",
    ),
    continuous: true,
  },
  {
    token: "border-width",
    unit: "px",
    reference: {
      value: 1,
      market: "тонкая граница у Primer — 0.0625rem, то есть тот же один пиксель при базовом кегле (сверено 2026-09-23)",
    },
    floor: unbounded(
      "толщина границы нормой не связана. 1.4.11 Non-text Contrast говорит про КОНТРАСТ различимой границы, а не про её толщину — тонкая, но контрастная граница норму держит",
    ),
    ceiling: unbounded(
      "сверху нормой не ограничена: толстая граница ничего не нарушает. Разумный верх задаёт сам элемент — граница шире половины его меньшей стороны съедает содержимое, но это геометрия, а не норма",
    ),
    continuous: true,
  },
  {
    token: "tracking",
    unit: "em",
    reference: {
      value: 0,
      market:
        "`tracking-normal` равен семени без смещения, а нормальный трекинг — это тот, который заложил в гарнитуру её автор: своего числа тут у рынка нет и быть не может (сверено 2026-09-23)",
    },
    floor: unbounded(
      "межбуквенный интервал нормой не связан. 1.4.12 Text Spacing требует, чтобы содержимое не ломалось, когда ПОЛЬЗОВАТЕЛЬ выставит свой интервал (0.12em) поверх нашего — это требование к вёрстке потребителя, а не граница нашего семени",
    ),
    ceiling: unbounded(
      "сверху не ограничен: разреженный текст нормы не нарушает. Где он перестаёт читаться — вопрос вкуса и гарнитуры, а не критерия, и решает его тот, кто крутил",
    ),
    continuous: true,
  },
];

export function axisOf(token: string): Axis | undefined {
  return AXES.find((axis) => axis.token === token);
}

