#!/usr/bin/env node
// scope-identity.mjs — SessionStart hook: инжектит identity-баннер по WEBCORE_SCOPE.
// Роль-модель (зоны/пины моделей/число архитекторов) — ДАННЫЕ из `.claude/harness.yaml`
//, НЕ хардкод. Роли-рамка (инварианты) — .claude/agents/{architect,owner,layer}.md.
//   - 'main' → architect;  <zone> → owner-<zone>;  невалид → anomaly;  ПУСТО → баннер «роли нет»
//     (не тишина: молчание на старте неотличимо от нормальной работы, BRAIN2-46 §3).
//
// Contract (SessionStart): stdout { hookSpecificOutput: { hookEventName, additionalContext } }.
// Subagents (Agent tool) SessionStart НЕ триггерят — их identity из subagent_type prompt'а.

import { argv } from "node:process";
import { fileURLToPath } from "node:url";
import {
  knownScopes,
  loadConfig,
  nearestScopes,
  needsOnboarding,
  overlappingZones,
  resolveScope,
  zoneReality,
} from "./harness-config.mjs";

// Признак незаполненного сида живёт в harness-config — одно знание на баннер и на доктора
// (BRAIN2-46 §1). Ре-экспорт: исторические импорты из этого модуля продолжают работать.
export { needsOnboarding };

function silent() {
  process.stdout.write("{}");
  process.exit(0);
}

function emit(additionalContext) {
  process.stdout.write(
    JSON.stringify({ hookSpecificOutput: { hookEventName: "SessionStart", additionalContext } }),
  );
  process.exit(0);
}

/**
 * Операционный минимум — ТЕКСТОМ в баннере, а не ссылкой на файл.
 *
 * Раньше баннер говорил «правила роли — `.claude/agents/shared-policy.md`» и считал дело
 * сделанным. Но `agents/*.md` — определения СУБАГЕНТОВ: роль-сессия (`WEBCORE_SCOPE=… claude`)
 * не грузит их ничем, и канон оставался файлом на диске, про который сессия знала только имя.
 * Отсюда весь набор жалоб разом: не читают доку зоны, не ходят в трекер, не сверяются с сетью,
 * пишут прозу в код. Правило, названное по имени, не исполняется — исполняется прочитанное.
 *
 * Здесь НЕ копия канона (вторая копия разъедется с первой молча, как всякий дубль): здесь то,
 * что должно сработать в первые же ходы, до того как сессия успеет наделать дел. Полный текст —
 * в корневом `CLAUDE.md`, который харнесс кладёт в контекст сам.
 */
function workOrderBlock(zoneDocs) {
  return [
    ``,
    `## До первого действия — порядок, не пожелание`,
    ``,
    `1. **Доку ${zoneDocs} — и доку КАЖДОЙ папки, в которую лезешь, — читаешь целиком до первой`,
    `   правки**: \`README.md\` (устройство), \`FAQ.md\` (почему так), \`ROADMAP.yaml\` (ТЗ: что сделано`,
    `   и что открыто), \`EXAMPLES.md\`/\`INDEX.md\` где есть. Есть у папки дока — значит в ней есть что`,
    `   документировать. Это держит хук \`docs-gate\`: до прочтения доступны только чтение и поиск —`,
    `   ни правки, ни Bash, ни MCP, ни субагентов. Целиком — частично открытый файл не считается.`,
    `2. **ТЗ — это \`ROADMAP.yaml\` скоупа, в котором работаешь**, не переписка и не трекер (трекер`,
    `   отражает эти же ямлы и расходится с ними молча). Нет пункта под работу — спроси user.`,
    `3. **Корневой \`CLAUDE.md\` уже у тебя в контексте** — там порядок работы, правила кода, MCP.`,
    `   Читай его, а не вспоминай, как принято. Инварианты роли — \`.claude/agents/shared-policy.md\`.`,
    ``,
    `## Пять самых дорогих ошибок здесь`,
    ``,
    `- **Не вываливай простыню.** user — человек, а не вторая модель. Нашёл пачку находок — отдай`,
    `  их пронумерованным списком, по фразе на пункт; разбор пишешь только по тому пункту, который`,
    `  он назвал, и по одному за раз. На уточнение отвечаешь по спрошенному, не добирая соседние темы.`,
    `- **Не делай поверх сказанного.** Просили одно — делаешь ровно это. Увидел рядом ещё три`,
    `  улучшения — назови их и спроси, не делай молча.`,
    `- **Не выдумывай.** Не знаешь API, адрес, чужой контракт — посмотри (код · дока · MCP · сеть)`,
    `  или скажи «не знаю». Правдоподобный ответ из головы принимают за проверенный.`,
    `- **Упёрся — скажи, что упёрся.** Костыль, обход, правка соседней зоны ради своего результата —`,
    `  запрещены. Это находка, и у неё есть владелец, а не повод импровизировать.`,
    `- **Сверяйся с сетью** перед новой механикой, развилкой и новой зависимостью — \`WebSearch\``,
    `  разрешён без спроса. Ответ из памяти модели сверкой НЕ является: она устаревает молча.`,
    `  След обязателен: что смотрел · что нашёл · почему берём · дата.`,
  ];
}

function modelLine(config, role) {
  const pin = config.models?.[role];
  return pin ? ` (модель-пин: \`${pin}\`)` : "";
}

function productLabel(config) {
  return config.product
    ? `продукта \`${config.product}\``
    : "этого продукта (имя не задано в `.claude/harness.yaml` → впиши `product:`)";
}

/**
 * Конфиг объявляет зоны, которых в этом репозитории нет НИ ОДНОЙ → он не отсюда (BRAIN2-46 §2).
 * Возвращает блок строк для баннера, либо `[]`. Идёт ПЕРВЫМ, до представления роли
 *: пока сомнение стоит после утверждения, читающий получает сначала
 * ложную личность, а потом оговорку к ней — и запоминает первую.
 */
export function foreignConfigWarning(config, cwd) {
  const reality = zoneReality(config, cwd);
  if (!reality.foreign) return [];
  const zones = [...new Set(reality.rows.map((r) => r.zone))].join(", ");
  return [
    `# ⚠️ КОНФИГ, ПОХОЖЕ, НЕ ОТ ЭТОГО РЕПОЗИТОРИЯ`,
    ``,
    `\`.claude/harness.yaml\` объявляет зоны (${zones}), и **ни одна их папка здесь не существует**`,
    `— объявлено путей: ${reality.declared}, найдено: 0. Так выглядит конфиг, скопированный из соседнего`,
    `продукта: имя продукта, роадмап и зоны в нём — чужие, а не «зоны ещё не созданы».`,
    ``,
    `**Action: STOP, спроси user.** Не иди в роадмап названного продукта и не заводи здесь`,
    `его зоны, пока не подтверждено, что конфиг верный. Разбор — \`node .claude/hooks/harness-doctor.mjs\`.`,
  ];
}

/**
 * Соседи по папке: зоны, чьи пути пересекаются с зоной этого scope. Владелец обязан узнать об
 * этом ИЗ БАННЕРА — диагностика говорит честно, но её никто не запускает,
 * а внутри пересечения машинной границы нет и от взаимного затирания ничто не защищает.
 * Возвращает блок строк, либо `[]`.
 */
export function overlapNotice(config, scope) {
  const pairs = overlappingZones(config).filter((pair) => pair.zones.includes(scope));
  const shared = pairs
    .filter((p) => p.kind === "equal")
    .map((pair) => {
      const self = pair.zones[0] === scope ? 0 : 1;
      return { zone: pair.zones[1 - self], mine: pair.paths[self], theirs: pair.paths[1 - self] };
    });
  // Вложенность читается с двух сторон по-разному: у одного внутри зоны чужая папка, у другого
  // своя зона лежит внутри чужой. Одна формулировка на оба случая дезориентирует и того, и того.
  const inside = pairs
    .filter((p) => p.kind === "nested" && p.zones[1] === scope)
    .map((p) => ({ zone: p.zones[0], path: p.paths[0] }));
  const within = pairs
    .filter((p) => p.kind === "nested" && p.zones[0] === scope)
    .map((p) => ({ zone: p.zones[1], path: p.paths[1] }));

  const lines = [];
  if (shared.length) {
    lines.push(
      ``,
      `## ⚠️ Ты делишь папку с другой зоной — границы между вами НЕТ`,
      ...shared.map((n) => `- с **owner-${n.zone}**: твой \`${n.mine}\` ↔ его \`${n.theirs}\``),
      ``,
      `Внутри пересечения governance пустит вас обоих: он держит границу зоны против ЧУЖИХ`,
      `папок, а не вас друг от друга. Значит от взаимного затирания не защищает ничто —`,
      `ни гейт, ни git (у вас общее дерево). Правишь общие файлы — сначала спроси user,`,
      `не идёт ли там работа. Пересечение не задумано — это вопрос к architect: сам`,
      `\`.claude/harness.yaml\` не правь, он вне твоей зоны.`,
    );
  }
  if (inside.length) {
    lines.push(
      ``,
      `## 📦 Внутри твоей зоны есть вложенная ЧУЖАЯ зона`,
      ...inside.map((n) => `- \`${n.path}/\` принадлежит **owner-${n.zone}**, не тебе`),
      ``,
      `Граница здесь машинная: владельцем файла считается зона с самым длинным совпавшим`,
      `путём, поэтому вложенная папка твоей НЕ является — гейт отобьёт и правку, и коммит.`,
      `Нужна работа внутри неё — это задача её владельцу, через architect.`,
    );
  }
  if (within.length) {
    lines.push(
      ``,
      `## 📦 Твоя зона лежит ВНУТРИ чужой папки`,
      ...within.map((n) => `- твоя папка вложена в \`${n.path}/\` (**owner-${n.zone}**)`),
      ``,
      `Это не значит, что сосед распоряжается твоими файлами: более специфичный путь выигрывает,`,
      `и внутри своей папки владелец — ты. Обратное тоже верно — наружу, в остальную его папку,`,
      `тебе нельзя.`,
    );
  }
  return lines;
}

/**
 * Состояние прогона — куда его класть, обвес больше НЕ говорит.
 *
 * Здесь стоял блок про чекпойнт роли с адресом узла в tasker. Служба снята (2026-08-24), и
 * вместе с ней снят блок: адрес, которого нет, хуже молчания — сессия тратит первый ход на
 * стук в мёртвую дверь, а протухший адрес выглядит как рабочий. Проверено на себе: баннер
 * слал в `PROBEWEB-15` воркспейса, которого не существует.
 *
 * Правило «пиши состояние прогона на закрытии этапа» никуда не делось, но живёт оно теперь
 * там, где живут задачи, а не в баннере: обвес отвечает за то, что проверяет машиной.
 */

/**
 * Готовый блок команд запуска — ОДИН формат на все случаи, когда сессию надо перезапустить
 * (роли нет · scope не резолвится). Второй формат тут был бы разным ответом на одно и то же
 * действие человека. Имена выровнены по ширине — список читается столбиком.
 */
export function launchBlock(scopes) {
  const width = Math.max(...scopes.map((s) => s.length));
  const cmd = (s) => `WEBCORE_SCOPE=${s.padEnd(width)} claude`;
  return [
    `\`\`\`sh`,
    ...scopes.map((s) =>
      s === "main" ? `${cmd(s)}   # architect — владеет доставкой` : `${cmd(s)}   # owner-${s}`,
    ),
    `\`\`\``,
  ];
}

/**
 * Здесь стоял адрес раздела пилотов в tasker/knowledger. Служб нет — строки нет.
 *
 * Обвес адресов больше не раздаёт: где живут задачи и знания, сессия узнаёт от user или из
 * инструментов, которые у неё реально есть. Выдуманный адрес дороже молчания.
 */

function onboardingBanner(config) {
  return [
    `# Session identity — WEBCORE_SCOPE=main (architect · ОНБОРДИНГ)${modelLine(config, "architect")}`,
    ``,
    `Ты **architect/main**, но \`.claude/harness.yaml\` — ещё НЕЗАПОЛНЕННЫЙ общий шаблон`,
    `(\`product: ${config.product ?? "(пусто)"}\`, зоны/пины — placeholder). Это НЕ аномалия и НЕ повод паниковать:`,
    `**твоя первая задача — ОНБОРДИНГ, не работа.** Заполни роль-модель под этот продукт ВМЕСТЕ с user.`,
    ``,
    `## Порядок онбординга`,
    `1. **Пойми проект** (known-места, не спрашивая других агентов): \`README\`/\`*.md\`, \`package.json\`/манифесты,`,
    `   \`git log\`/структура папок. Определи реальные пакеты/папки — будущие зоны.`,
    `2. **Спроси user, где живут задачи и знания продукта** — трекер, вики, что угодно. Обвес адреса`,
    `   не знает и не выдумывает; работать по локальным файлам вместо трекера нельзя.`,
    `3. **Предложи user конфиг** и заполните \`.claude/harness.yaml\` вместе: \`product\`, \`zones\` (\`paths[]\``,
    `   из РЕАЛЬНЫХ папок, disjoint), \`models\` (по факту сессии). Решения по продукту — за **user**.`,
    `4. **Проект пустой** (нечего изучать) → поговори с user: что строим, какие зоны в планах → засидь под это.`,
    ``,
    `## Пока сид не заполнен`,
    `- **Owner'ов НЕ поднимаем** — governance режет по placeholder-путям, owner упрётся в стену.`,
    `- **Непонятно — спрашивай USER** (не другого агента; агенты друг друга не зовут).`,
    `- Проверка после заполнения: \`node .claude/hooks/harness-doctor.mjs\`.`,
    `- Обвязку (\`.claude/\`) — закоммить в репу; артефакты установки`,
    `  (\`agent-harness-plugin/\`, демо-папки) — НЕ коммить (gitignore).`,
  ].join("\n");
}

function architectBanner(config) {
  return [
    `# Session identity — WEBCORE_SCOPE=main (architect)${modelLine(config, "architect")}`,
    ``,
    `Ты в роли **architect/main** ${productLabel(config)}. Правила роли — \`.claude/agents/architect.md\` + \`.claude/agents/shared-policy.md\`.`,
    `Роль-модель — данные \`.claude/harness.yaml\` (архитекторов сконфигурено: ${config.architects}).`,
    ``,
    `- Триаж запросов user; задача овнеру — **пункт \`ROADMAP.yaml\` его скоупа** (трекер отражает эти же ямлы, истина в них).`,
    `- **НЕ пиши код зон сам** — ставишь задачу овнеру → owner-сессию запускает user.`,
    `- Локальных \`briefs/\`-файлов НЕ заводим: вторая копия ТЗ разъезжается с первой молча.`,
    `- Git: **владеешь доставкой** — ветка → PR → влитие, вливаешь только ты (права даёт marker \`.claude/.main-session-id\`).`,
    `  Это не обход правил: главная ветка защищена, прямой коммит в неё отказывают и тебе — заводи ветку и PR.`,
    `- Отдавая ТЗ овнеру, назови git-обвязку: **ветку заводишь ТЫ** (owner'у \`git checkout -b\` режет гейт), коммит ему можно, пуш/мерж — нет.`,
    `- Отражение работы в трекере ведёшь ты, а не овнер: его дело — роадмап своего скоупа.`,
    ...workOrderBlock("зоны, в которую смотришь,"),
  ].join("\n");
}

function ownerBanner(config, { scope, paths, name }) {
  const list = paths.length
    ? paths.map((p) => `\`${p}/\``).join(", ")
    : "`(зона без путей — аномалия конфига)`";
  const firstPath = paths[0] ?? "<зона>";
  return [
    `# Session identity — WEBCORE_SCOPE=${scope} (owner-${scope})${modelLine(config, "owner")}`,
    ``,
    `Ты в роли **owner-${scope}** ${productLabel(config)}, владелец зоны: ${list} (${name}).`,
    `**Ты НЕ architect** — правила роли architect не твои.`,
    ``,
    `## Зона (boundary)`,
    `- Edits — ТОЛЬКО внутри своих папок: ${list}. Любой файл вне них → STOP, верни state architect.`,
    `- Машинная граница (governance-хук) блокирует Edit/Write вне этих путей — не обходи, эскалируй ВВЕРХ.`,
    `- **Коммит тоже ограничен зоной** (с 2026-09-18): \`git add .\` в общем working tree заметал`,
    `  незакоммиченную работу соседей, и она уезжала в твой коммит. Теперь git-gate считает, что`,
    `  реально попадёт в коммит, и отбивает чужие файлы. Собирай коммит поимённо: \`git add ${firstPath}\`.`,
    ...overlapNotice(config, scope),
    ``,
    `## Правила (канон)`,
    `- Первым читаешь \`.claude/agents/shared-policy.md\`.`,
    `- **НЕ пиши ADR**, не принимай cross-zone решения — это architect. Упёрлось → STOP + эскалация.`,
    `- **Git: commit-only** (под git-gate). Push/merge — architect после ревью. Conventional: \`feat(${scope}): ...\`.`,
    `- **Ветку заводит architect, не ты** (\`git checkout -b\` режет гейт): работай в той, что названа в ТЗ; не названа — спроси user.`,
    `- **Версию пакета не поднимаешь** — это architect при публикации; назови в комменте к задаче, какую считаешь правильной.`,
    `- Хук заблокировал git — НЕ обходи (\`bash -c\`/\`&&\`/\`--no-verify\`). STOP + эскалация.`,
    `- POLICY priority 0: никаких костылей, причина не следствие, DoD = код+тесты+трейсы+доки.`,
    ``,
    `## Скоуп задачи`,
    `ТЗ — пункт \`ROADMAP.yaml\` твоего скоупа (у вложенного скоупа свой роадмап, у зоны свой) либо текст от user.`,
    `Статус пункта проставляется РЯДОМ с работой, в момент когда она сделана. Непонятен scope — STOP, спроси. Не угадывай.`,
    `Отчёт отдаёшь туда же, откуда пришло ТЗ: в роадмап — статусом и записью, user'у — текстом.`,
    ...workOrderBlock("своей зоны"),
  ].join("\n");
}

/**
 * Scope не резолвится. Чаще всего это ОПЕЧАТКА, а не отсутствующая зона, поэтому порядок
 * совета такой же, как у CLI рынка: сначала «перезапустись под верным именем» с готовой
 * командой, и только потом «а если зоны правда нет — впиши её в конфиг». Прежний порядок вёл
 * человека править конфиг из-за одного лишнего символа.
 */
export function anomalyBanner(config, scope) {
  const scopes = knownScopes(config);
  const near = nearestScopes(scope, config);
  const lines = [
    `# Session identity — WEBCORE_SCOPE=${scope} (UNRESOLVED)`,
    ``,
    `**Аномалия**: scope "${scope}" не резолвится в зону (нет в \`.claude/harness.yaml\`).`,
    ``,
  ];
  if (near.length === 1) {
    lines.push(
      `Похоже на опечатку — **возможно, ты имел в виду \`${near[0]}\`**. Перезапусти сессию:`,
      ``,
      ...launchBlock(near),
      ``,
      `Не то — вот все доступные:`,
      ``,
    );
  } else if (near.length > 1) {
    lines.push(
      `Похоже на опечатку. Близкие по написанию: ${near.map((s) => `\`${s}\``).join(", ")} —`,
      `выбери нужный сам (за тебя не выбираем) и перезапусти сессию:`,
      ``,
      ...launchBlock(near),
      ``,
      `Не то — вот все доступные:`,
      ``,
    );
  } else {
    lines.push(
      `**Перезапусти сессию под верным scope** (похожего на "${scope}" среди них нет):`,
      ``,
    );
  }
  lines.push(
    ...launchBlock(scopes),
    ``,
    `Зоны действительно нет (это не опечатка) — тогда впиши её в \`.claude/harness.yaml\``,
    `(секция \`zones:\`) и перезапустись. Случай редкий: сперва проверь имя выше.`,
    ``,
    `**Action**: STOP. Сообщи user — scope невалидный. Не начинай работу (нет boundary/ownership).`,
  );
  return lines.join("\n");
}

/**
 * `WEBCORE_SCOPE` не задан. Раньше здесь была тишина: гейты закрывались правильно, но
 * человек узнавал об этом на первой правке, а пустой ответ хука неотличим от нормы
 * (BRAIN2-46 §3). Говорим сразу и с готовой командой запуска.
 */
export function noScopeBanner(config) {
  const scopes = knownScopes(config);
  return [
    `# Session identity — РОЛИ НЕТ (\`WEBCORE_SCOPE\` не задан)`,
    ``,
    `Сессия стартовала **без роли**: harness не знает, кто ты, и потому закрывает обе двери —`,
    `любая правка файла и любая git-запись получат \`deny\` с причиной «boundary неизвестна».`,
    `Это не поломка: гейты работают штатно, роли просто нет.`,
    ``,
    `**Action: STOP, не начинай работу.** Перезапусти сессию с ролью:`,
    ``,
    ...launchBlock(scopes),
    ``,
    scopes.length > 1
      ? `Доступные scope: ${scopes.join(", ")} (из \`.claude/harness.yaml\`).`
      : `Зон в \`.claude/harness.yaml\` нет — доступен только \`main\`. Разбор: \`node .claude/hooks/harness-doctor.mjs\`.`,
  ].join("\n");
}

/**
 * Сомнение вперёд, представление роли — под ним и с оговоркой. Порядок и есть содержание:
 * баннер, который сначала уверенно называет чужой продукт, а сомневается ниже, читается как
 * «ты architect продукта X, но есть нюанс» — то есть личность уже принята.
 */
function withForeignWarning(banner, config, cwd) {
  const warning = foreignConfigWarning(config, cwd);
  if (!warning.length) return banner;
  return [
    ...warning,
    ``,
    `---`,
    ``,
    `Ниже — представление роли **по этому же конфигу**: имя продукта, зоны и пины взяты`,
    `из него, и доверия ему сейчас нет. Читай как гипотезу, а не как свою личность.`,
    ``,
    banner,
  ].join("\n");
}

function main() {
  const cwd = process.cwd();
  const config = loadConfig(cwd);
  const scope = process.env.WEBCORE_SCOPE;
  if (!scope) return emit(withForeignWarning(noScopeBanner(config), config, cwd));
  if (scope === "main") {
    const banner = needsOnboarding(config) ? onboardingBanner(config) : architectBanner(config);
    return emit(withForeignWarning(banner, config, cwd));
  }
  const resolved = resolveScope(scope, config);
  if (resolved?.kind !== "zone") return emit(anomalyBanner(config, scope));
  return emit(withForeignWarning(ownerBanner(config, resolved), config, cwd));
}

// Исполняем main() ТОЛЬКО как скрипт (main вызывает process.exit) — при import безопасно.
if (fileURLToPath(import.meta.url) === argv[1]) {
  try {
    main();
  } catch {
    silent();
  }
}
